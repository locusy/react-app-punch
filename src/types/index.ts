export interface IStoreState {
    languageName: string;
    enthusiasmLevel?: number;
}

export interface UserPermiseState {
    UserPermiseList: Object;
}

export interface MenuFlatState {
    menuFlatArr: Array<any>;
}